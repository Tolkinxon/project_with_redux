const initialState = {
    news: [],
    newsLoadingStatus: 'tolkinxon',
    filters: []
}

const reducer = (state = initialState, action) => {
    switch({ type, payload }){
        case '' : 
            return
        case '' : 
            return
        default:
            return state
    }
}